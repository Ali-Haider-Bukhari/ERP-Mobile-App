### First for installing node modules

```npm i ``` 
Then for running react-native project 
```npm start```
 

use only react-native commponents otherwise in case of any third-party library tell other colaborators first.
